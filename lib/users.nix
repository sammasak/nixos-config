# User identity configuration
# Define user-specific data here (git credentials, etc.)
# Referenced by flake.nix when defining hosts

{
  lukas = {
    git = {
      userName = "sammasak";
      email = "23168291+sammasak@users.noreply.github.com";
    };
  };
}
